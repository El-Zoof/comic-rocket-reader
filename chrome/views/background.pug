doctype html

html
  head
    // MV3 note: This background page template is no longer used.
    // The manifest now declares a service_worker pointing directly to
    // js/background.bundle.js — no HTML wrapper is needed or loaded.
    // This file is kept only so the build process does not error;
    // it is safe to leave it in place.
